__pycache__/

# Temporary reference files (Anti-compaction strategy)
tmp_cleanup/
.tmp-*

# Zen MCP Server data (should live in ~/.zen-mcp, not in repos)
data/promptcraft/

# Claude local config files (may contain secrets/tokens)
.claude/.mcp.json
.claude/settings.local.json
